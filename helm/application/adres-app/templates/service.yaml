apiVersion: v1
kind: Service
metadata:
  name: {{ include "adres-app.fullname" . }}
  labels:
    {{- include "adres-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
    {{- end }}
  selector:
    {{- include "adres-app.selectorLabels" . | nindent 4 }}
